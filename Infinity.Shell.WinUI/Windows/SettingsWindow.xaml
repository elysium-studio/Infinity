<Window
    x:Class="Infinity.Shell.WinUI.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Shell="using:Infinity.Shell"
    xmlns:WinUI="using:Elysium.UI.WinUI">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TitleBar x:Name="AppTitleBar" Title="Settings">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="ms-appx:///Assets/Infinity.png" />
            </TitleBar.IconSource>
        </TitleBar>
        <NavigationView
            Grid.Row="1"
            Margin="0,8,0,0"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleButtonVisible="False"
            IsSettingsVisible="False"
            MenuItemsSource="{x:Bind ViewModel, Mode=OneWay}"
            PaneDisplayMode="Left"
            SelectedItem="{x:Bind ViewModel.SelectedItem, Mode=TwoWay}">
            <NavigationView.Resources>
                <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent" />
                <SolidColorBrush x:Key="NavigationViewContentGridBorderBrush" Color="Transparent" />
                <Thickness x:Key="NavigationViewContentGridBorderThickness">0</Thickness>
            </NavigationView.Resources>
            <NavigationView.MenuItemTemplate>
                <DataTemplate x:DataType="Shell:ISettingViewModel">
                    <NavigationViewItem Margin="8,0,0,0">
                        <ContentControl
                            HorizontalContentAlignment="Stretch"
                            Content="{x:Bind}"
                            ContentTemplateSelector="{WinUI:ViewTemplateSelector}" />
                    </NavigationViewItem>
                </DataTemplate>
            </NavigationView.MenuItemTemplate>
            <ContentControl
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch"
                Content="{x:Bind ViewModel.SelectedItem, Mode=OneWay}">
                <ContentControl.ContentTransitions>
                    <TransitionCollection>
                        <ContentThemeTransition />
                    </TransitionCollection>
                </ContentControl.ContentTransitions>
                <ContentControl.ContentTemplate>
                    <DataTemplate x:DataType="Shell:ISettingViewModel">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0" Margin="36,0,36,16">
                                <Grid.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontFamily" Value="Segoe UI Variable Display" />
                                        <Setter Property="FontSize" Value="28" />
                                        <Setter Property="FontWeight" Value="SemiBold" />
                                    </Style>
                                </Grid.Resources>
                                <ContentControl Content="{x:Bind}" ContentTemplateSelector="{WinUI:ViewTemplateSelector}" />
                            </Grid>
                            <ScrollViewer Grid.Row="1">
                                <ItemsControl
                                    Margin="36,0,36,36"
                                    ItemTemplateSelector="{WinUI:ViewTemplateSelector}"
                                    ItemsSource="{x:Bind}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Spacing="4" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </DataTemplate>
                </ContentControl.ContentTemplate>
            </ContentControl>
        </NavigationView>
    </Grid>
</Window>