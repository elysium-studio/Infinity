<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="Infinity.Shell.WinUI.TrackedWindowCollectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Behaviors="using:CommunityToolkit.WinUI.Behaviors"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:Shell="using:Infinity.Shell"
    xmlns:Triggers="using:CommunityToolkit.WinUI"
    xmlns:WinUI="using:Elysium.UI.WinUI"
    DataContext="{WinUI:ViewModel Key=TrackedWindowCollectionView}"
    IsTabStop="False"
    Loaded="{x:Bind ViewModel.Activate}"
    Unloaded="{x:Bind ViewModel.Deactivate}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Dark">
                    <SolidColorBrush
                        x:Key="LayerOnAcrylicPrimaryBackgroundBrush"
                        Opacity="0.5"
                        Color="{ThemeResource SolidBackgroundFillColorBase}" />
                </ResourceDictionary>
                <ResourceDictionary x:Key="Light">
                    <SolidColorBrush
                        x:Key="LayerOnAcrylicPrimaryBackgroundBrush"
                        Opacity="0.5"
                        Color="{ThemeResource SolidBackgroundFillColorBase}" />
                </ResourceDictionary>
                <ResourceDictionary x:Key="HighContrast">
                    <SolidColorBrush x:Key="LayerOnAcrylicPrimaryBackgroundBrush" Color="{ThemeResource SystemColorWindowColor}" />
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border
        x:Name="Root"
        Width="1000"
        Background="{ThemeResource LayerOnAcrylicPrimaryBackgroundBrush}">
        <Interactivity:Interaction.Behaviors>
            <Behaviors:KeyDownTriggerBehavior Key="Tab">
                <Interactivity:InvokeCommandAction Command="{x:Bind ViewModel.SelectNextCommand}" />
            </Behaviors:KeyDownTriggerBehavior>
            <Behaviors:KeyDownTriggerBehavior Key="Enter">
                <Interactivity:InvokeCommandAction Command="{x:Bind ViewModel.ActivateSelectedCommand}" />
            </Behaviors:KeyDownTriggerBehavior>
        </Interactivity:Interaction.Behaviors>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Padding="6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="5*" />
                        <ColumnDefinition Width="2*" />
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="1" VerticalAlignment="Center">
                        <TextBox
                            Padding="44,5,6,6"
                            HorizontalAlignment="Stretch"
                            CornerRadius="16"
                            PlaceholderText="Find a desktop window..."
                            Text="{x:Bind ViewModel.FilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                            <TextBox.Resources>
                                <ResourceDictionary>
                                    <ResourceDictionary.ThemeDictionaries>
                                        <ResourceDictionary x:Key="Dark">
                                            <StaticResource x:Key="TextControlBackground" ResourceKey="ControlFillColorDefaultBrush" />
                                            <StaticResource x:Key="TextControlBackgroundPointerOver" ResourceKey="ControlFillColorSecondaryBrush" />
                                            <StaticResource x:Key="TextControlBackgroundFocused" ResourceKey="ControlFillColorInputActiveBrush" />
                                            <StaticResource x:Key="TextControlBorderBrush" ResourceKey="ControlElevationBorderBrush" />
                                            <StaticResource x:Key="TextControlBorderBrushPointerOver" ResourceKey="ControlElevationBorderBrush" />
                                            <StaticResource x:Key="TextControlBorderBrushFocused" ResourceKey="ControlElevationBorderBrush" />
                                        </ResourceDictionary>
                                        <ResourceDictionary x:Key="Light">
                                            <StaticResource x:Key="TextControlBackground" ResourceKey="ControlFillColorDefaultBrush" />
                                            <StaticResource x:Key="TextControlBackgroundPointerOver" ResourceKey="ControlFillColorSecondaryBrush" />
                                            <StaticResource x:Key="TextControlBackgroundFocused" ResourceKey="ControlFillColorInputActiveBrush" />
                                            <StaticResource x:Key="TextControlBorderBrush" ResourceKey="ControlElevationBorderBrush" />
                                            <StaticResource x:Key="TextControlBorderBrushPointerOver" ResourceKey="ControlElevationBorderBrush" />
                                            <StaticResource x:Key="TextControlBorderBrushFocused" ResourceKey="ControlElevationBorderBrush" />
                                        </ResourceDictionary>
                                    </ResourceDictionary.ThemeDictionaries>
                                </ResourceDictionary>
                            </TextBox.Resources>
                        </TextBox>
                        <ImageIcon
                            Width="18"
                            Height="18"
                            Margin="14,0,0,0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            IsHitTestVisible="False"
                            Source="ms-appx:///Assets/Infinity.ico" />
                    </Grid>
                </Grid>
                <Button
                    VerticalAlignment="Center"
                    IsTabStop="False"
                    ToolTipService.ToolTip="Options">
                    <Button.Resources>
                        <SolidColorBrush x:Key="ButtonBackground" Color="Transparent" />
                        <SolidColorBrush x:Key="ButtonBackgroundDisabled" Color="Transparent" />
                        <SolidColorBrush x:Key="ButtonBorderBrush" Color="Transparent" />
                    </Button.Resources>
                    <Button.Style>
                        <Style BasedOn="{StaticResource DefaultButtonStyle}" TargetType="Button">
                            <Setter Property="Width" Value="32" />
                            <Setter Property="Height" Value="32" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="HorizontalAlignment" Value="Right" />
                            <Setter Property="VerticalAlignment" Value="Top" />
                            <Setter Property="BorderBrush" Value="Transparent" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="FontSize" Value="16" />
                            <Setter Property="Content" Value="&#xE712;" />
                            <Setter Property="FontFamily" Value="Segoe Fluent Icons" />
                        </Style>
                    </Button.Style>
                    <Button.Flyout>
                        <MenuFlyout Placement="Bottom">
                            <MenuFlyoutItem Click="{x:Bind ViewModel.NavigateToSettings}" Text="Settings">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE713;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem Click="{x:Bind ViewModel.NavigateToTour}" Text="Take a tour">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE7BE;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem Click="{x:Bind ViewModel.NavigateToAbout}" Text="About">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE946;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutSeparator />
                            <MenuFlyoutItem Click="{x:Bind ViewModel.ExitApplication}" Text="Exit">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE7E8;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                        </MenuFlyout>
                    </Button.Flyout>
                </Button>
            </Grid>
            <Grid
                Grid.Row="2"
                Padding="0,12,0,12"
                Background="{ThemeResource LayerOnAcrylicFillColorDefault}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,0">
                <Border
                    x:Name="BackdropBorder"
                    Width="{x:Bind ViewModel.ViewportIndicatorWidth, Mode=OneWay}"
                    VerticalAlignment="Stretch"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource OverlayCornerRadius}"
                    SizeChanged="HandleBackdropSizeChanged">
                    <Border Background="{x:Bind ToSolidColorBrush(ViewModel.BackgroundColour), Mode=OneWay}">
                        <Image Source="{x:Bind ToImageSource(ViewModel.BackgroundPath), Mode=OneWay}" Stretch="UniformToFill" />
                    </Border>
                </Border>
                <ScrollViewer
                    VerticalAlignment="Stretch"
                    HorizontalScrollBarVisibility="Hidden"
                    IsTabStop="False"
                    TabNavigation="Once"
                    VerticalScrollBarVisibility="Disabled">
                    <ItemsControl
                        Width="{x:Bind ViewModel.CanvasWidth, Mode=OneWay}"
                        IsTabStop="False"
                        ItemsSource="{x:Bind ViewModel, Mode=OneWay}"
                        TabNavigation="Once">
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="IsTabStop" Value="False" />
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="Shell:TrackedWindowViewModel">
                                <ContentControl
                                    Content="{WinUI:View Key=TrackedWindowView}"
                                    IsTabStop="False"
                                    TabNavigation="Once" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                <PipsPager
                    x:Name="PageIndicator"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Bottom"
                    IsTabStop="False"
                    SelectedIndexChanged="OnSelectedPageChanged"
                    TabNavigation="Once" />
            </Grid>
        </Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="Compact">
                    <VisualState.StateTriggers>
                        <Triggers:IsEqualStateTrigger Value="{x:Bind ViewModel.PreviewSizeIndex, Mode=OneWay}" To="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="Root.Height" Value="156" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Default">
                    <VisualState.StateTriggers>
                        <Triggers:IsEqualStateTrigger Value="{x:Bind ViewModel.PreviewSizeIndex, Mode=OneWay}" To="1" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="Root.Height" Value="200" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Large">
                    <VisualState.StateTriggers>
                        <Triggers:IsEqualStateTrigger Value="{x:Bind ViewModel.PreviewSizeIndex, Mode=OneWay}" To="2" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="Root.Height" Value="256" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Border>
</UserControl>